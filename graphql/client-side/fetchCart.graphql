query FetchExistingCart($input: String!) {
  userCart(userId: $input) {
    id
    userItems {
      item {
        itemId
        itemDescription
        itemImage
        itemName
        itemPrice
        itemQuantity
      }
    }
  }
}
